# Simple Node.js Web Application

This is a minimal Node.js web application using the built-in `http` module.

## Run locally

1. Open a terminal in `nodejs-application`
2. Run `npm install` (not required for this app, but useful for future dependencies)
3. Run `npm start`
4. Open `http://127.0.0.1:3000` in your browser
